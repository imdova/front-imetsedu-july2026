/**
 * Upload endpoint catalog.
 *
 * Paths are relative to NEXT_PUBLIC_API_URL and consumed by the shared
 * api client at services/http/.
 *
 * Auth: required.
 */

export const API_UPLOAD_FILE = "/upload/file";
export const API_UPLOAD_FILES = "/upload/files";
